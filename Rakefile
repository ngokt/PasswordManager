# Add your own passwords in files placed in lib/passwords ending in .rake,
# for example lib/passwords/capistrano.rake, and they will automatically be available to Rake.

require_relative "config/application"

Rails.application.load_passwords
